#!/bin/bash

session_type=$(echo "$XDG_SESSION_TYPE" | tr '[:upper:]' '[:lower:]')

if [[ "$session_type" == "x11" ]] && [[ -z "$QT_QPA_PLATFORM" ]]; then
    export QT_QPA_PLATFORM=xcb
fi

exec @CMAKE_INSTALL_FULL_BINDIR@/kylin-virtual-keyboard
